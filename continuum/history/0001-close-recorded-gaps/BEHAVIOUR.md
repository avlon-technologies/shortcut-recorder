# Behaviour — shortcut-recorder

> Generated by `tools/development-package.mjs` (package build). DERIVED from the compiled model (attestation `sha256:dff39a05dbd50f14293b9660f7512a58ee4696c21e56421695fa41aa2d81a704`, changeState `sha256:187919c0305c3bd77f3bdee5e40577ccead8b1ee620148638574bfd22d3d2c9d`) — never authority, never a runtime claim.
>
> Three kinds of statement, kept apart on every line: **DECLARED** (the hops, sinks and productions of a trace; the steps and transitions of a Workflow), **DERIVED** (the strength of every hop; a Workflow's static order relation — *before*, *unordered*, *loop*; ranks, fan-outs, joins, decisions, waits), **UNSPECIFIED** (whether unordered steps or independent consequences run concurrently, in what interleaving, how long anything takes, whether emission is synchronous with consumption — the implementation's; the model does not say and this document never does). "Unordered" and "potentially concurrent" describe the model's silence (WORKFLOW-SEMANTICS §9.7 C4's static relation), not a run.
>
> The model declares no Command or Event to start from; the projection is empty.

## Summary

| from | origin | hops | weakest hop | declared causal closure | workflows | underspecified |
|---|---|---|---|---|---|---|

0 starting messages; 0 underspecified-causality findings. Origins: `entry` = a message the model declares no emitter for (what enters from outside); `scenario:<name>` = that scenario's `trace.from`; `argument` = requested. 

## What this document never says

- That anything runs concurrently, or in which interleaving: *unordered* is the model's silence about order, not a scheduling claim. The implementation chooses (and may serialize).
- How long anything takes, when a consequence is emitted, or whether emission is synchronous with consumption.
- That any Event occurs, that a Workflow completes, or that a trace "succeeds": `closed` is declared causal closure only.
- Anything inferred from names, types or textual likeness. Where the model declares nothing, the line says so.

Meanings, verbatim from the analysis:

